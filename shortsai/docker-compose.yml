services:
  shortsai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - shortsai_storage:/app/backend/storage
    restart: unless-stopped

volumes:
  shortsai_storage:
